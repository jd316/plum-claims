# Self-Improvement Proposals (advisory only)

The system reads its OWN evaluation outputs (decision eval, extraction robustness, confidence calibration, confidence config) and proposes concrete improvements. **ADVISORY ONLY**: nothing here changes a prompt, threshold, weight, or decision. The `auto_applicable` flag is informational — no code path applies a proposal. The decision pipeline and the 12 cases are untouched.

## Findings (current metrics)

### Decision layer
- Cases: **630**  ·  overall accuracy: **100.0%**
- Amount MAE: ₹0.0000 (max ₹0.0000)  ·  reason-code accuracy: 100.0% (n=290)
- Mismatching templates: **none** (all 100%)

### Extraction
- CORD total-match: **73.3%** (n=15, mean rel-err 0.266, median 0.0)
- Systematic miss: Non-INR receipts using '.'/',' as THOUSANDS separators (IDR locale) are misread as decimals (60.000 -> 60.0): 4/15 CORD misses, rel-err ~1.0, high confidence. INR (the product domain) does not use this convention.
- Handwriting read accuracy: **70.0%** (n=10)

### Calibration
- ECE: **0.1442** before / **0.0** after isotonic fit (improvement 0.1442)
- [0.9,1.0] bin: accuracy 0.828 vs mean conf 0.964 -> **over-confident**
- Calibration applied: **False** (calibrator present: True, method: isotonic); ECE-after is in-sample (n=31).

### Confidence config (read-only)
- Weights: extraction 0.3, rules 0.3, completeness 0.2, verifier 0.2 (sum 1.0)  ·  degradation penalty 0.2

## Proposals

### 1. [decision_rules] — risk: low — auto-applicable

- **Observation:** Per-template decision accuracy is 100% on 630 synthetic cases (all templates clean; amount-MAE ₹0.0000; reason-code accuracy 100%).
- **Proposed change:** No rule/threshold/weight change indicated. To keep coverage honest, EXPAND the synthetic set with boundary cases (amounts exactly AT each limit/sub-limit/threshold) and adversarial templates (conflicting rule signals, near-miss dates) so 100% stays meaningful.
- **Rationale:** Clean metrics on the current templates do not prove robustness at decision boundaries; adding boundary + adversarial fixtures stresses the rules without touching them — purely additive coverage.

### 2. [extraction_prompt] — risk: low — human-review-required

- **Observation:** Extraction misreads thousands-separated totals on non-INR receipts (4/15 CORD misses, rel-err ~1.0, total-match 73%, mean confidence 0.96).
- **Proposed change:** Add a number-normalization instruction to the extraction prompt: 'Interpret the document's locale; treat '.'/',' as thousands separators when the magnitude/format implies it (e.g. printed 60.000 on a receipt = 60000, not 60.0).' Validate on the CORD set before any rollout — DO NOT auto-apply.
- **Rationale:** The misses are a systematic locale artefact (digits read faithfully, separator misinterpreted), not random OCR error, so a targeted prompt nudge directly addresses the pattern. INR bills are unaffected, so the 12 cases stay green. Confidence is also high on these misses, which feeds the calibration finding below.

### 3. [extraction_fixtures] — risk: low — auto-applicable

- **Observation:** Handwriting read accuracy is 70% on 10 RxHandBD crops — the failure mode is near-miss spellings (e.g. 'inderen' -> 'Inderer').
- **Proposed change:** Expand the handwriting probe set and add a confusable-character post-check (fuzzy-match readings against the policy's known medicine vocabulary) as an ADVISORY confidence signal. Adding fixtures/an advisory check does not change any decision.
- **Rationale:** Handwritten medicine names are inherently ambiguous; more labelled crops make the read-accuracy number trustworthy, and a vocabulary check surfaces likely misreads without overriding the extractor.

### 4. [confidence_calibration] — risk: medium — human-review-required

- **Observation:** Confidence is over-confident in the [0.9,1.0] bin (accuracy 0.83 vs mean conf 0.96, n=29); ECE 0.14 before / 0.00 after the isotonic fit. Calibration is currently OFF (default).
- **Proposed change:** Enable the fitted calibration map (settings.confidence_calibration_enabled=True) in production ONCE VALIDATED on held-out data. The map is already committed and inert; flipping the flag is reversible and never changes the weights/penalty.
- **Rationale:** An ECE of 0.14 means '0.96' overstates correctness (83% actual) — calibration makes the score statistically meaningful. Risk is MEDIUM because the current fit is in-sample on a small set (n=31); validate on logged real outcomes at volume and report held-out ECE before enabling.

---

_Generated by `app.services.self_improve`. Advisory only — no proposal is auto-applied; `auto_applicable` is informational and is never True for a change that would alter a decision threshold, rule, weight, or a confidence-affecting flag without human review._
